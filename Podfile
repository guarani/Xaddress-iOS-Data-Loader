# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'XaddressLoader' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for XaddressLoader
  pod 'SwiftCSV', '~> 0.3'

  target 'XaddressLoaderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'XaddressLoaderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
